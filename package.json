{
  "name": "buddy-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run dev:chrome",
    "dev:chrome": "vite",
    "dev:firefox": "vite --mode firefox",
    "build": "npm run build:chrome",
    "build:chrome": "vite build && node scripts/build-content.mjs chrome",
    "build:firefox": "vite build --mode firefox && node scripts/build-content.mjs firefox",
    "build:all": "npm run build:chrome && npm run build:firefox",
    "package:chrome": "npm run build:chrome && node scripts/verify-release.mjs chrome && node scripts/package.mjs chrome",
    "package:firefox": "npm run build:firefox && node scripts/verify-release.mjs firefox && node scripts/package.mjs firefox",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "verify": "node scripts/verify-release.mjs",
    "release:check": "npm run typecheck && npm run test && npm run build:all && npm run verify",
    "package:all": "npm run package:chrome && npm run package:firefox"
  },
  "dependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.7.0",
    "@tailwindcss/vite": "^4.0.0",
    "@types/chrome": "^0.0.268",
    "@vitejs/plugin-vue": "^6.0.7",
    "happy-dom": "^20.10.6",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.4.0",
    "vite": "^8.0.16",
    "vitest": "^4.1.9",
    "vue-tsc": "^3.3.5",
    "web-ext": "^8.0.0"
  }
}
